<header role="banner" id="navhead" class="fixed-top">
  <nav role="navigation" id="mainNav" class="navbar navbar-dark bg-dark navbar-expand-md" aria-label="Main navigation">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
          <span class="h3 mb-0">Point de Vente Orange CI</span>
        </a>
      <div class="navbar-collapse justify-content-end collapse" style="float: right;">
        <ul class="navbar-nav" >
          <li class="nav-item dropdown">
            <button type="button" class="nav-link btn btn-link btn-inverse dropdown-toggle d-flex align-items-center" data-toggle="dropdown">
              <span class="svg-avatar mr-1" aria-hidden="true"></span>
              {{ app.session.get('username')|upper }}
            </button>
            <ul class="dropdown-menu dropdown-menu-right">
              <li>
                <form action="{{path ('logout')}}" method="post" style="margin: 0px;">
                  <button type="submit" class="dropdown-item">Se déconnecter</button>
                </form>

              </li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>